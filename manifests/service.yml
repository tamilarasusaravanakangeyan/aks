apiVersion: v1
kind: Service
metadata:
    name: tamilarasusaravanakangeyanaks
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: tamilarasusaravanakangeyanaks